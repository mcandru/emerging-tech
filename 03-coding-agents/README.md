TODO:

- add example of `AGENTS.md` to add instructions to ensure that there are no unused imports
- add example of `AGENTS.md` to add instructions to ensure that packages are added via shell command rather than from memory to get latest versions
